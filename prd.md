---
id: PRD-001
title: Agent Memory — Claude Code 本地自动记忆
version: 1.1
status: implementation-baseline
updated: 2026-09-10
---

# Agent Memory 项目 PRD

本文按用户最新决定收敛首版：**Claude Code＋MCP＋Skill，全自动，本地模型，统一本地数据库；网页只有安装说明。** 本文取代前期方案中的管理后台、远程模型、多个宿主和复杂部署范围。当前尚未实现或实测；数据库与模型候选在 M0 验证后锁定。

## 1. 产品定义与已经确定的范围

一个安装后自动工作的 Claude Code 记忆工具。它在本地记录项目经历、提炼可复用记忆、为新任务提供相关上下文，并自动聚合和维护过时信息。用户日常继续使用 Claude Code，无需管理记忆列表、挑选模型或维护数据库。

| 项目 | 首版决定 |
|---|---|
| 宿主 | Claude Code 本地客户端；先验收终端模式 |
| 用户入口 | 一个网页，内容仅为添加 Skill 的说明、安装步骤及必要的首次下载提示 |
| 能力入口 | 一个 MCP 服务，提供检索、记录与少量维护操作 |
| 自动触发 | 安装时一并配置轻量生命周期 hooks；用户不用每轮调用 Skill |
| 推理 | 首次启动自动下载固定版本的本地模型；记忆生成与 embedding 都在本机执行 |
| 数据 | 一个本地数据库保存文本、向量、关系、状态与处理记录；模型权重作为本地模型文件保存 |
| 数据库候选 | LanceDB 优先验证；关系以实体与边数据表达，不把它宣称为原生属性图数据库 |
| 首发硬件基线 | 暂定 macOS Apple Silicon、16 GB 内存；实际版本与资源上限在 M0 实测 |
| 日常交互 | 自动运行；查询状态、纠正、删除、暂停可通过 Skill 命令完成 |
| 首版不包含 | 登录、管理后台、记忆图谱页面、云同步、团队共享、远程推理、第二宿主 |

“自动”指安装完成后的采集、处理、召回、聚合与老化不需要逐次操作。首次安装仍遵循 Claude Code 自身的工具与 hook 信任机制；不绕过宿主权限。

## 2. Problem Statement：解决什么问题

用户反复向 Claude Code 解释同一个项目的工具、约束和历史决定。成功排障没有沉淀成带条件的经验；已有摘要又容易丢失来源、例外和版本信息。环境变化后，旧经验仍可能被召回。

首版聚焦三个结果：减少重复交代；找回与当前项目相关的经历；及时停止使用已知不适用的记忆。网站只承担安装入口，产品价值发生在 Claude Code 的日常使用中。

## 3. Solution：最短用户路径

1. 用户打开网页，按说明添加记忆 Skill 并完成一次安装。
2. 安装器注册 MCP 与必要 hooks，在本地建立工作目录。安装范围和本地处理说明与安装命令放在同一页面。
3. 首次启动自动检查并下载固定模型，显示下载进度；完成后自动开始记忆处理。
4. 用户正常在项目中使用 Claude Code。系统自动保存允许采集的事件，在后台生成记忆。
5. 下一任务或新会话自动获得简短、相关、有来源的记忆。
6. 用户需要时在 Claude Code 内执行状态、纠正、删除或暂停命令；无需另开管理后台。

首版按当前明确工作区绑定项目。未能识别唯一项目时，使用会话范围并返回原因，不猜测其他目录。只处理宿主提供的输入与输出，不扫描整台电脑或自动导入全部历史。

## 4. User Stories

1. 作为 Claude Code 用户，我希望按一个页面的说明完成安装，以便快速开始使用。
2. 作为用户，我希望模型自动下载和初始化，以便无需研究本地推理配置。
3. 作为用户，我希望下载中断后能继续，以便不重复下载完整文件。
4. 作为用户，我希望模型就绪后离线生成和检索记忆，以便无需提供云模型密钥。
5. 作为用户，我希望记忆自动按项目区分，以便同名项目或不同工作区不会串用经验。
6. 作为用户，我希望任务自动获得相关项目事实，以便减少重复说明。
7. 作为用户，我希望成功与失败经历都能保留，以便系统学习适用条件和反例。
8. 作为用户，我希望不同措辞也能找到同类问题，以便无需记住原话。
9. 作为用户，我希望错误码与工具名称能准确命中，以便保留精确检索能力。
10. 作为用户，我希望重复内容自动合并，以便记忆不会无限重复。
11. 作为用户，我希望经验保留来源、否定和例外，以便它不会变成过度概括的建议。
12. 作为用户，我希望环境变化后旧经验退出默认召回，以便新任务不继续采用过时做法。
13. 作为用户，我希望明确纠正后新查询立即生效，以便不反复纠正同一事实。
14. 作为用户，我希望用 Skill 命令查看依据和状态，以便需要时理解自动行为。
15. 作为用户，我希望暂停、恢复和删除可在 Claude Code 内完成，以便无需维护另一个界面。
16. 作为用户，我希望本地模型繁忙或不可用时主任务继续，以便记忆功能不阻断工作。
17. 作为用户，我希望多个 Claude Code 会话共享同一份可靠本地记忆，以便不会重复下载或互相覆盖。
18. 作为维护者，我希望能按时间顺序回放任务并检查结果，以便验证聚合和老化有没有帮助。

## 5. 网页与安装形态

网页只有一个安装说明页面：一句产品说明、一组添加 Skill 的步骤、可复制的安装入口、首次模型下载和本地硬件要求、安装成功的检查方式。下载失败和卸载方式可作为同页短说明，不扩展成设置中心。

首版无需 React 管理应用、数据库浏览器、登录页、记忆列表或图谱。安装命令、包名和分发地址由实现阶段确定；发布前必须实际可运行，PRD 不提供不存在的包名。

安装包包含三类内容：Skill 教 Claude 使用记忆能力，MCP 暴露工具，hooks 触发确定的自动采集和注入。可将三者作为同一 Claude Code 插件分发，也可以由安装器写入受支持的对应配置；M0 选择一条并固定，不同时维护两种安装方式。Claude Code 官方支持插件携带 hooks 和 MCP 配置。[插件参考](https://code.claude.com/docs/en/plugins-reference)

Skill 的自动调用依赖模型判断相关性，不能保证每次任务都会调用。因此“全自动”的可靠入口由生命周期 hooks 提供，MCP 和 Skill 负责能力与交互；只有 Skill 正文的安装不算自动闭环完成。[Skill 调用机制](https://code.claude.com/docs/en/skills#control-who-invokes-a-skill)

## 6. 运行架构与自动注入

| 部分 | 职责 | 用户是否管理 |
|---|---|---|
| MCP 启动入口 | 与 Claude Code 通信；连接或启动本机唯一记忆内核 | 否 |
| Skill | 指导按需检索、查看状态、纠正和删除；说明记忆材料的使用边界 | 只需安装 |
| 轻量 hooks | 捕获可用事件，在会话和任务入口取得上下文 | 随安装配置 |
| 本地内核 | 数据库、任务队列、检索、聚合、生命周期、跨会话一致性 | 否 |
| 本地推理进程 | 加载生成模型与 embedding 模型，执行限定任务 | 自动启动、卸载 |
| 静态网页 | 安装说明 | 安装时使用 |

### 6.1 自动入口

| 时机 | 行为 |
|---|---|
| MCP 启动 | 快速完成协议握手，检查模型和数据库，后台开始缺失资源下载 |
| SessionStart | 绑定项目，提供少量当前项目事实；压缩恢复时补充必要任务状态 |
| UserPromptSubmit | 记录用户输入，按当前任务召回并注入少量记忆 |
| PostToolUse / PostToolUseFailure | 记录允许采集的结果、错误与环境，不将工具成功等同任务成功 |
| Stop | 保存回合结束信息，触发增量整理，不自动宣称任务已完成 |

以上入口依据 Claude Code 官方文档，发布时须对固定客户端版本验证实际事件和返回语义。[Hooks 参考](https://code.claude.com/docs/en/hooks)

hooks 只做短操作：接收事件、限时读取、返回上下文。模型处理在后台完成。下载、模型加载和整库整理不能阻塞 MCP 握手或 Claude Code 的当前任务。

### 6.2 本机进程生命周期

每个 Claude Code 会话可有自己的 MCP 连接，但本机同一用户只有一个记忆内核持有数据库写入权。启动器通过本地锁和受限 IPC 复用内核，避免多次下载、重复模型常驻和并发写冲突。

最后一个会话关闭后，内核最多用 60 秒完成可结束的小批处理，然后保存队列并退出；未完成工作在下次启动恢复。老化可在读取时按当前时间计算，维护在下次运行继续，首版不承诺关机或全部会话关闭后仍全天工作。

协议输出与诊断日志分离。MCP STDIO 的 stdout 只输出协议内容，下载进度写入 stderr 或通过状态工具返回。

## 7. 本地模型与首次启动

统一数据库不意味着所有工作使用同一个模型。首版自动管理两套权重：生成模型用于抽取、归纳和冲突候选；embedding 模型用于将查询和记忆转成检索向量。用户不用手动选择。

| 角色 | 首版验证候选 | 用途 |
|---|---|---|
| 生成模型 | Qwen3-1.7B 官方 GGUF，Q8_0 | 将事件提炼成结构化事实、经历和经验候选 |
| 向量模型 | Qwen3-Embedding-0.6B 官方 GGUF，Q8_0 | 中文、英文和混合文本的本地向量表示 |
| 推理运行时 | llama.cpp，固定经过验收的发布版本 | 同一运行时体系加载上述两类模型；按需管理进程 |

模型组合是待实测的工程默认，不宣称其质量已足够。若结构化抽取或中文检索未通过 M0，调整量化或模型型号；不静默改为调用远程推理服务。

官方文件页显示生成权重约 1.83 GB、embedding 权重约 639 MB，合计约 2.47 GB，另需运行时、临时下载与数据库空间。[生成模型文件](https://huggingface.co/Qwen/Qwen3-1.7B-GGUF/blob/main/Qwen3-1.7B-Q8_0.gguf)、[向量模型文件](https://huggingface.co/Qwen/Qwen3-Embedding-0.6B-GGUF/blob/main/Qwen3-Embedding-0.6B-Q8_0.gguf)

模型卡标注两模型采用 Apache-2.0，llama.cpp 使用 MIT；发行包保留对应声明并锁定版本。[生成模型卡](https://huggingface.co/Qwen/Qwen3-1.7B-GGUF)、[向量模型卡](https://huggingface.co/Qwen/Qwen3-Embedding-0.6B-GGUF)、[llama.cpp](https://github.com/ggml-org/llama.cpp)

### 7.1 自动下载契约

- 首次启动读取随发布固定的模型清单：仓库、文件名、精确 revision、大小、SHA-256、许可证与对应运行时版本。
- 下载到临时文件，支持断点续传；校验通过后原子切换为可加载文件，不能加载半成品。
- 已有正确文件不重复下载。多个会话竞争时只有一个下载任务，其余读取状态。
- 自动检查剩余空间；不足、网络断开或校验失败时返回具体原因与可恢复状态，不进入反复下载循环。
- 模型下载网络请求只用于获取权重和运行资源；记忆抽取、聚合和 embedding 不调用远程推理服务，产品不启用正文遥测。召回后交付给 Claude Code 的片段仍进入 Claude 的处理链路；“本地记忆”不表示 Claude Code 整体离线。
- 模型升级随应用发布显式版本化，不每次启动追随最新权重；embedding 升级需要重新建索引。
- 下载前在安装说明明确总下载量和本地空间要求。模型文件大小不能等同运行内存；运行内存与上下文上限单独实测。

### 7.2 就绪、降级与资源限制

状态依次为初始化、下载中、模型加载中、就绪；失败可转为仅记录 / 文本检索模式。模型未就绪时先可靠记录事件，后台排队；不把“已收到”显示成“已生成记忆”。

生成默认并发 1，处理有限长度事件批次，长工具结果分片并保留截断标记。默认每批生成输入上限 4,096 tokens、输出上限 768 tokens，必要时对原始片段分批处理；这些是初始配置，需按模型模板计数。

生成采用非思考模式与结构化输出约束；格式、来源引用与适用条件分别验证。embedding 固定模型版本、1024 维与对应查询模板，M0 验证 pooling 和归一化配置后写入模型清单。

前台向量查询优先级高于后台生成。模型忙、加载慢或检索超时时采用文本和结构过滤降级，并记录原因。闲置后卸载生成模型，避免仅为等待任务长期占满内存。首版不进行自动微调、不执行模型生成的任意维护代码。

## 8. 本地数据库选型与图的边界

### 8.1 LanceDB 可以作为候选

LanceDB OSS 可以嵌入本地应用，提供 TypeScript 接口，保存文本、结构字段和向量，并进行全文、向量和混合检索。它的 Apache-2.0 许可证与本地使用方式适合这一安装简单的产品。[官方仓库](https://github.com/lancedb/lancedb)、[本地接入](https://docs.lancedb.com/quickstart)、[混合检索](https://docs.lancedb.com/search/hybrid-search)

需要区分“保存图信息”和“原生图数据库”：首版图信息是实体与明确关系，如来源、替代、支持、反例和适用条件。LanceDB 可以保存这些记录，由应用完成受控关系追溯；本次官方接口核查未确认它提供原生属性图或 Cypher 多跳查询。向量索引内部使用图结构不等于提供业务知识图。

| 候选 | 对首版的适配 | 主要代价 | 当前结论 |
|---|---|---|---|
| LanceDB | 本地嵌入、文本＋向量＋结构数据、混合检索 | 应用维护边、依赖撤回与发布一致性 | 优先做 M0 验证 |
| SurrealDB 嵌入式 | 原生关系遍历、全文、向量与事务 | 原生打包与核心 BSL 许可条款须按发布版本核实 | 原生图能力成为硬要求时优先评估 |
| LadybugDB | 本地图模型与查询，全文 / 向量扩展 | 扩展分发、版本兼容和增量索引行为需验证 | 备用技术候选 |

比较依据：[SurrealDB Node 引擎](https://surrealdb.com/docs/reference/javascript/engines/node)、[SurrealDB 索引](https://surrealdb.com/docs/reference/query-language/statements/define/indexes)、[核心许可证](https://github.com/surrealdb/surrealdb/blob/main/LICENSE)、[LadybugDB](https://github.com/LadybugDB/ladybug)、[全文扩展](https://docs.ladybugdb.com/extensions/full-text-search/)、[向量扩展](https://docs.ladybugdb.com/extensions/vector/)。

### 8.2 选定条件

LanceDB 进入实施验证，但尚不写成已经定案。必须用本地 OSS、实际发行版 Node SDK 验证以下能力，不能以 Cloud / Enterprise 或底层其他接口代替：

1. 在同一数据库内可靠保存事件、记忆、来源关系和控制状态。
2. 在纠正或删除后，全文、向量和关系查询一致排除旧内容。
3. 单写入内核与重放机制能承受崩溃，不能向读者发布半完成变更。
4. 新写入即使尚未重建索引也可正确读取；不使用跳过未索引片段的加速设置来换取失真。
5. 删除覆盖旧数据版本、保留标签和分支；删除最新行不能冒充全部清理完成。首版不提供用户创建数据库标签或分支的功能。
6. 在目标 Mac 上干净安装，无用户手动编译数据库依赖。

已核查的本地 Node `mergeInsert` 提供同表事务语义；本次未确认通用多表事务入口。因此 M0 优先验证单个权威记录集合的原子批量发布；派生索引不能拥有独立的事实状态。若这个设计使生命周期实现明显复杂，则改选具有对应事务能力的统一引擎，不为维持 LanceDB 名称而补入第二个权威数据库。[Node 表接口](https://github.com/lancedb/lancedb/blob/main/nodejs/lancedb/table.ts)、[连接接口](https://github.com/lancedb/lancedb/blob/main/nodejs/lancedb/connection.ts)

LanceDB 数据修改产生新版本，旧快照及 tag 可能继续保留数据；物理清理必须包含版本保留处理。默认读取一致性也需要明确设置或刷新，同一内核统一访问有助于约束问题。[版本管理](https://docs.lancedb.com/tables/versioning)、[读取一致性](https://docs.lancedb.com/tables/consistency)

## 9. 数据模型与处理流程

### 9.1 必备对象

| 对象 | 必备内容 |
|---|---|
| Project | 稳定 ID、可信工作区映射、相关环境、启用 / 暂停状态 |
| Event | 幂等事件 ID、来源根、会话 / 任务 / 项目、角色、发生与接收时间、正文及截断标记 |
| Memory | ID、版本、类型、正文、范围、状态、条件、来源、有效期、模型与策略版本 |
| Relation | 两端 ID 与版本、关系类型、来源、有效性；不从“同时出现”直接推出因果 |
| Job | 输入源版本、处理阶段、重试和发布状态；属于数据库内可靠数据 |
| ContextBundle | 本次选取的记忆版本、原因、长度与交付结果；不将交付等同采用 |
| Control / Tombstone | 纠正与删除代次、被撤回来源标识；删除后不保留敏感正文 |

Memory 类型先实现事实、明确偏好、任务经历和条件化经验。所有关系及向量都能映射回稳定来源。全文与向量是查找工具；最后的范围和有效性检查依赖权威状态。

### 9.2 写入与聚合

事件可靠落盘 → 本地生成模型抽取结构化候选 → 校验来源、范围和格式 → 查相关旧记忆 → 去重 / 冲突处理 → 生成正文、条件与来源关系 → 校验输入版本 → 原子发布记忆、来源依赖与控制状态 → 异步维护可重建索引。来源关系必须与记忆一起生效，不能先公开正文再补关系；向量未就绪允许检索降级。

后台默认在回合结束或积累 20 条新事件时处理增量。一次原始事件的重试、摘要和导出只算一个证据根；归纳从原始证据重新生成，保留数字、否定、例外和适用版本。

明确用户事实可以生效；从引用、第三方材料或模型自述推导的偏好不得当作用户指令。无法确定的内容保持候选。每个生成字段须能对应来源；JSON 格式正确不代表内容正确。

一次成功经历可以被召回为“曾发生过的结果”，但不能直接升级成普遍规则。经验自动晋升的初始门槛为：至少 3 个独立任务、跨 2 个会话，有与目标相关的可核验结果，适用条件和例外完整，且无未解释反例。次数只是去重后的最低门槛；工具退出码或模型自评不能单独证明成功。

### 9.3 召回

确定项目与任务 → 关键词 / 向量查候选 → 补充来源和条件关系 → 检查当前状态与环境 → 合并排序 → 生成有限上下文 → 交付 Claude Code。

首版每条检索通道最多 20 个候选，默认最多交付 6 条、总计不超过 800 tokens，来源使用短指针。实现时固定计数器；精确错误码优先保留原文。无合格结果返回空包；候选、不适用、已删除和被替代记忆不能作为当前建议提供。

历史记忆正文始终作为有来源的材料，不能因为来自数据库而提升为宿主行为规则。注入内容标注范围、时间与不确定性；未验证的外部指令不能混入集成规则。

## 10. 自动演进、老化与控制

### 10.1 状态与规则

| 状态或动作 | 首版行为 |
|---|---|
| 候选 | 可继续积累证据；不作为默认行动建议注入 |
| 有效 | 通过当前项目、条件与来源检查后可被选取 |
| 待复核 | 有反例、冲突或依赖环境变化；退出默认召回 |
| 被替代 | 保留历史，但不当作当前事实 |
| 归档 | 移出常用召回；只有显式历史查询可以返回 |
| 删除 | 先禁止读取，再清理来源、派生物和旧版本 |
| 固定 | 仅豁免时间降权 / 自动归档，不覆盖纠正和失效 |

首版自动演进指更新内容与适用条件，不能修改自身程序、提示词策略或执行任意自创技能。策略有版本，发布前检查来源版本和当前控制代次。

初始老化规则：事实与明确偏好不按时间自动变假；普通经历召回时间权重半衰期 30 天，经验为 90 天；未固定且 180 天没有明确复用反馈的经历可归档。临时任务状态在任务完成后退出当前状态，7 天未更新进入待复核。数字是待验证的初始配置，不是研究结论；没有默认物理自动删除策略。

已知到期、明确纠正或相关环境变化采用硬过滤，不等待时间衰减。召回次数不能提高事实可信度。环境检查只比较经验明确依赖的字段，不因无关文件变化撤回所有记忆。

### 10.2 Skill 中的维护能力

命令名称为产品交互示意，实际命名遵循最终插件或 Skill 命名空间：

| 用户意图 | 功能 |
|---|---|
| 查看记忆状态 | 模型是否就绪、当前项目、队列、最近错误与降级原因 |
| 为什么记住这件事 | 返回当前记忆版本、来源及变更原因 |
| 纠正某条记忆 | 通过明确用户操作形成新版本，立即排除已确定冲突旧版本 |
| 忘记某段经历 | 显示来源与派生影响范围，执行屏蔽与清理 |
| 暂停 / 恢复记忆 | 停止 / 恢复自动采集、召回与后台发布 |

MCP 直接写入的来源由可信入口标记，不能由模型填写 `user_confirmed` 就变成用户明确要求。普通自由文本纠正经异步抽取处理；明确用户命令的纠正才承诺同步确认语义。

首版可信维护入口固定为：UserPromptSubmit 对顶层、符合预定语法的用户显式 Skill 维护命令进行识别，由本地内核签发一次性意图凭据，绑定项目、操作、目标版本与原始参数。manage 校验并消费凭据，不能扩大操作范围；无凭据的模型调用只能登记候选或返回需要明确命令的状态。不从引用文本或一般自然语言推断此凭据。删除先以只读预览返回影响范围，用户明确提交该预览编号后执行；这一过程在 Claude Code 内完成。

纠正确认后启动的新查询必须采用新版本；旧向量未更新也不能召回已确定冲突的事实。交付前检查控制代次，过时包重新获取或跳过。已经进入 Claude 历史或发送给模型的内容无法追改。

删除先撤回来源及所有依赖结论；首版以整条来源事件为最小清理单位，执行前说明可能同时影响其他记忆。清理覆盖正文、向量、关系内容、缓存和数据库旧版本；多来源结论先撤回，再仅凭未删来源重新计算。只保留无正文的防重放标识，旧后台任务不能复活已删内容。用户重新提供同义内容是新输入，不保证永久识别所有重述。

本地删除不包含 Claude 历史、用户手工导出和系统备份，不宣称存储介质取证级擦除。暂停立即禁止新采集和新发布，在途结果丢弃或安全中止；恢复不补采暂停期间内容。

## 11. Implementation Decisions：模块与接口

首版采用 TypeScript 构建 MCP、安装器、领域内核和 hooks 适配；本地推理使用经过验收的 llama.cpp 子进程。Node 版本跟随所选数据库发行包要求并固定，不直接跟踪仓库主分支。网页使用静态页面。

| 模块 | 公开职责 |
|---|---|
| Installer / Bootstrap | 安装与卸载集成、单实例启动、模型清单与下载恢复 |
| Claude Adapter | 可信项目映射、事件归一化、短 hook、上下文交付 |
| Memory Service | 唯一业务入口，统一身份、状态、范围和版本检查 |
| Unified Store | 事件、记忆、关系、向量、队列、控制状态的持久化契约 |
| Local Models | 结构化抽取、向量生成、资源管理；不直接修改数据库 |
| Memory Worker | 去重、聚合、晋升、老化、派生撤回与任务恢复 |
| Retrieval | 混合召回、有效性过滤、来源补充和上下文预算 |

首版 MCP 工具控制在少量稳定操作：

| 工具 | 输入 | 输出或保证 |
|---|---|---|
| recall | 任务查询、可信项目范围、当前 / 历史模式 | 上下文包、源版本、降级原因；允许正常空结果 |
| record | 事件或候选记忆、来源与任务信息 | 持久接收编号和处理状态；不接受伪造用户来源 |
| feedback | 任务、上下文包、可核验结果 | 反馈记录；不会直接以此任意晋升规则 |
| inspect | 状态查询或记忆标识 | 下载 / 模型 / 队列状态，或记忆解释 |
| manage | 明确用户动作、目标、预期版本 | 纠正、删除、固定、暂停或恢复的结果与覆盖范围 |

hooks 与 MCP 复用 Memory Service；不各自实现一套写入、检索或纠错逻辑。安装器为连接赋予工作区权限，模型输入的 project ID 不能授予额外访问权。本地 IPC 限于当前用户，模型服务端口若使用 HTTP 则仅绑定回环且加访问凭据。

错误至少区分模型准备中、下载失败、磁盘不足、暂停、未授权、版本冲突、源已删除、超时与处理失败。服务不可用时 hook 继续宿主任务并记录失败；只有可靠落盘后才能确认接收，不承诺数据库损坏时仍可保存新事件。

每个事件使用稳定幂等 ID，任务按来源版本去重。暂未完成的事件由数据库队列恢复；不增加第二个权威数据库。诊断日志只保存标识、计时与错误类别，不保存对话正文。上下文包内保存正文时必须纳入删除范围。

默认排除凭据文件和已识别的密钥字段，只摄入宿主允许且在项目范围内的内容。截断、过滤或采集失败都保留状态，不以完整事件自居；过滤规则不宣称能识别全部敏感信息。

## 12. Testing Decisions：验收方式

以 Memory Service 的外部行为作为主要测试边界：输入事件或用户动作，观察查询结果、交付版本及重启后状态。测试使用真实本地数据库与可控时钟，不通过直接改表伪造预期结果。

模型输出用可重放夹具检查业务一致性；另用真实本地模型评测抽取与检索质量。宿主只保留一组真实端到端冒烟，验证安装、事件入口与交付。当前没有已有实现或测试可复用；从这三个层次建立基线，不为每个内部函数另建镜像测试。

| 编号 | 场景 | 必须达到的行为 |
|---|---|---|
| AC-01 | 干净环境首次启动 | 自动下载并校验模型；MCP 可以响应状态，不等待整个下载才握手 |
| AC-02 | 断网、校验失败、磁盘不足 | 明确失败阶段；可恢复；不加载残缺权重，不无限重下 |
| AC-03 | 模型已经缓存后断网 | 本产品可完成抽取、向量生成、聚合与检索；不要求 Claude 工作流整体离线 |
| AC-04 | 安装一个 Skill 集成包 | 一个实际 Claude Code 版本完成自动采集与新任务注入，不要求每轮手动调用 |
| AC-05 | 两个会话与两个同名项目 | 共享一个本地内核和模型下载任务；项目内容不互串 |
| AC-06 | 同一事件重试 10 次 | 只产生一次逻辑事件，不重复增加经验支持数 |
| AC-07 | 接收确认后强杀服务 | 重启恢复已确认事件与未完成任务，不发布半完成状态 |
| AC-08 | 明确纠正成功、向量尚未更新 | 新查询及下一个受控交付排除已确定冲突旧事实 |
| AC-09 | 普通引用或模型推断 | 不自动写成用户明确偏好；来源和不确定性正确 |
| AC-10 | 合并相似但条件不同的经历 | 保留版本、否定、例外及来源，不错误增加独立证据 |
| AC-11 | 经验出现反例或相关环境升级 | 进入待复核或失效，退出默认召回 |
| AC-12 | 时间快进与重要固定记忆 | 类型化降权 / 归档正确；固定不覆盖语义失效 |
| AC-13 | 聚合过程中删除来源 | 默认读取立即屏蔽；旧任务和重启不能复活；旧版本清理可核查 |
| AC-14 | 模型生成中暂停 | 不再采集、召回或发布；恢复不补采暂停期间内容 |
| AC-15 | 最后会话退出 | 限时保存工作并退出模型进程；下次启动自动恢复 |
| AC-16 | 模型忙或检索超时 | Claude 主任务继续；明确文本降级或空包，不交付未经校验的旧包 |

首版准备 40 条按时间顺序回放的任务轨迹、160 个查询检查点，分布于至少两个项目；覆盖中文、英文、代码混合文本、纠正、重复、反例、环境变化、删除与恢复。评测答案不写入记忆。

在这组固定测试中，跨项目泄漏、已删除来源再现、已确定冲突旧事实被当作当前事实提供必须为 0 次；这只说明固定集通过，不表述为对任意输入的绝对保证。对聚合预先标注的关键否定、例外和版本条件必须全部保留。

质量目标：结构化抽取中可核对字段的准确率 ≥95%，关键来源引用有效率 100%，相关记忆 Recall@6 ≥90%。这些是初始发布目标，需报告样本数和实际计数；小模型不达标时不得仅以格式合法宣称完成。

性能目标在一台公开配置的 Apple Silicon / 16 GB 机器上测试，使用 20,000 条事件、5,000 条记忆、两个并行会话：热召回端到端 p95 ≤500 ms，hook 硬期限 1,000 ms，超时降级。计时包含查询 embedding、范围检查和适配开销；冷模型加载单列，不混入热态结果掩盖。记录 RSS、峰值磁盘、生成耗时和 Claude 同机运行的影响，未实测前不承诺最低内存。

产品价值用固定模型与上下文预算对比“无长期记忆”“只找原始经历”“聚合＋老化”三组，记录重复纠正次数、任务成功、额外耗时与资源消耗。首版不承诺固定成功率提升，也不让自动参数调优进入发布范围。

## 13. 实施顺序与完成标准

按纵向闭环推进，每一步都可演示。下列是依赖顺序，不是未经估算的工期承诺。

| 里程碑 | 工作 | 可演示结果与退出标准 |
|---|---|---|
| M0：锁定本地基础 | 验证 LanceDB 原子发布、关系撤回、索引新旧数据和历史清理；测试两个官方模型与运行时；固定 macOS、Node、数据库、模型 revision | 一次完整“存入→文本 / 向量查找→纠正→删除→强杀重启”通过；模型句对与字段抽取通过小样本门槛；记录选型结果 |
| M1：第一个 Claude 闭环 | 建立 MCP、唯一内核、一个最小 Skill 集成与必要 hooks；事件持久化、项目隔离、明确事实召回、暂停和基础删除 | 项目中记住一条事实，新会话自动获得；一个真实客户端版本通过 AC-04 至 AC-08 |
| M2：自动模型准备与记忆生成 | 正式接入模型清单、下载 / 恢复 / 校验、资源控制、本地抽取与混合检索 | 空缓存开始，全流程无需手工挑模型；离线记忆处理通过，失败可恢复 |
| M3：聚合、演进与老化 | 来源去重、条件化经验、反例降级、版本变化、类型化老化、完整派生删除与旧版本清理 | 多次独立经历形成候选 / 有效经验；环境变化撤回；删除过程中旧任务不能复活内容 |
| M4：可安装试用版 | 完成唯一安装网页、可复制真实安装流程、发行制品、状态 / 纠正命令、卸载、固定回归集 | 新用户按一个页面安装成功；模型自动就绪；全部硬门槛通过且如实报告质量和性能 |

首个可领取任务：**M0 本地存储与模型可行性验证**。交付数据库实际发行版及平台记录、最小持久化回归、索引更新与删除结论、模型质量样本和资源结果。允许在 M0 更换数据库候选，不允许为跳过失败检查而弱化纠正与删除语义。

M1 可在 M0 的存储契约确定后开始；安装网页文案与 MCP 契约可以并行准备。完整应用实现和公开发布属于后续执行，本文不表示它们已经完成。

## 14. Out of Scope

- 管理后台、记忆编辑器、关系图浏览页面与原生桌面壳。
- Codex、Cursor、Gemini 等第二宿主接入。
- 云同步、多设备冲突合并、团队权限和账号体系。
- 远程记忆推理、模型训练、自动调参、自主执行生成的技能。
- 通用图查询产品、任意 Cypher 或 SQL 控制台。
- 浏览器、邮件、文件系统等额外资料抓取与全量历史导入。
- 首发未验收的系统 / 架构支持，以及关机后持续学习。

## 15. 决策状态与依据

| 项目 | 状态 |
|---|---|
| Claude Code、MCP、Skill、安装说明单页、全自动、本地模型自动下载、统一本地存储 | 用户已明确 |
| 随安装加入 hooks、单写入内核、会话退出后限时排空、少量 Skill 维护命令 | 为达成上述目标所作实施决策 |
| LanceDB | 用户提出的候选；官方能力已核查，本地一致性与图关系实现待 M0 实测 |
| 两个 Qwen 模型＋llama.cpp | 官方模型与文件已核查；本产品质量、速度及内存待 M0 实测 |
| 原生多跳图数据库 | 当前未设为首版硬要求；若必须由数据库直接完成，则重新选择 SurrealDB / LadybugDB |
| 数值阈值与资源预算 | 初始设计参数，可经固定测试调整并记录版本 |

前期背景：[市场调研](/Users/chenxin/Documents/Codex/2026-09-10/zuo/outputs/ai-agent-memory-research.md)、[产品形态与运行机制研究](/Users/chenxin/Documents/Codex/2026-09-10/zuo/outputs/agent-memory-product-design.md)。这些文档用于保留依据；首版范围与实施要求以本 PRD 为准。

